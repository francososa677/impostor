{
  "name": "impostor-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Impostor - Social deduction real-time game",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"server,web\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev -w @impostor/server\" \"npm run dev -w @impostor/web\"",
    "dev:server": "npm run dev -w @impostor/server",
    "dev:web": "npm run dev -w @impostor/web",
    "build": "npm run build -w @impostor/shared && npm run build -w @impostor/game-engine && npm run build -w @impostor/server && npm run build -w @impostor/web",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck -w @impostor/shared && npm run typecheck -w @impostor/game-engine && npm run typecheck -w @impostor/server && npm run typecheck -w @impostor/web",
    "validate-data": "tsx scripts/validate-data.ts",
    "check": "npm run validate-data && npm run typecheck && npm run test && npm run build",
    "start": "node apps/server/dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "concurrently": "^8.2.2",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
